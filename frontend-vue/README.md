# frontend-vue

Vue 3 + Vite starter that talks to the Node backend by default (proxy configured).

Run (PowerShell):
```
cd frontend-vue
npm install
npm run dev
```

Open the URL printed by Vite (usually `http://localhost:5173`).
